# JREC-01 / jyu-gas-ver3.1 現状確認記録 — 2026-06-03

実装前の現状確認（read-only）結果の正式記録。
本ファイルは「保険対応アプリ（JREC-01）を進める前に、後日作業が重複しないように現在地を固定する」ことを目的とする。

> このフェーズでは **コード修正・本番データ書込・申請書生成・施術録生成・GAS push / deploy・Cloud Run deploy を一切行っていない**。read-only 確認と docs 記録のみ。

---

## 1. 確認サマリ

| 項目 | 値 |
|---|---|
| 確認日 | 2026-06-03 |
| repo path | `C:\hirayama-ai-workspace\workspace\gas-projects\jyu-gas-ver3.1` |
| branch | `main`（※ CLAUDE.md 記載の `feature/auto-dev-phase3-loop` ではない。この repo の正本は `main`）|
| HEAD | `a90c4d6` feat(staff): 全 staff 7 page に「平山ビジネスポータルへ戻る」追加 @17 (TASK-PORTAL-LINK-AUDIT-003) |
| pull結果 | `git pull --ff-only` → "Already up to date." |
| working tree | clean（`git status --short` 空） |
| ahead / behind | 0 / 0（origin/main と完全一致） |
| missing tracked | なし（`git ls-files -d` 空 → 2026-05-14 dirty 事故の再発なし。clasp push 安全ゲートはクリア） |
| remote | `github.com/dabu-pi/jyu-gas-ver3.1.git` |

---

## 2. 正本マップ（いま何が正本か）

| 対象 | 正本 | 派生物（正本でない） |
|---|---|---|
| コード本体・仕様・docs | **GitHub `dabu-pi/jyu-gas-ver3.1` の `main`** | GAS deployment `@N` / ローカル disk（乖離時は GitHub を信用） |
| 来院・患者の運用データ | JREC-01 本体スプレッドシート（container-bound SS・**URL/ID 未特定**） | — |
| 申請書 帳票出力 | **B案 = Cloud Run Excel**（`V3TR_menuGenerateApplication_B`） | A案 Sheets直PDF（**停止・本番化しない**）/ Python ローカルは補助 |
| 正式帳票テンプレ | `tools/claim-excel/application_template.xlsx`（新 様式第5号） | — |
| 価格マスタ | JBIZ SS `1FnJdALwFSv48WiD6NWr0DzG78kwB692R2pFeiTcZlCc`（外部参照） | — |

出典: `docs/JYU_GAS_SOURCE_OF_TRUTH_2026-05-14.md` / `docs/CLAIM_APPLICATION_OUTPUT_ROUTE_DECISION_2026-05-07.md`

---

## 3. 主要ファイル

| ファイル | 役割 |
|---|---|
| `Ver3_core.js`（307KB） | 来院登録・区分判定・30日ルール・近接部位・doGet ルーティング |
| `Ver3_amounts.js`（55KB） | 金額計算・長期減額・多部位逓減・月内上限 |
| `Ver3_transferData.js`（132KB） | 申請書転記データ・B案 Cloud Run 連携・`_申請書生成ログ` 記録 |
| `Ver3_shuRecorder.js`（78KB） | 施術録 月次出力（Google Docs テンプレ複製） |
| `Ver3_outputManager.js` | 申請書/施術録の月次出力フォルダ統一（01_申請書 / 02_施術録） |
| `Ver3_patientPicker.js` / `Ver3_test.js` / `Ver3_smokeTest.js` | 患者選択UI / テスト |
| `tools/claim-excel/` | write_application.py / application_template.xlsx / verify_application_xlsx.py / README.md |
| `.clasp.json` | scriptId: `1LROlc63TPr4Y2uV3nT6tAwOc-T0bOFflF0aXNzTlLEM_C3QbydHxCTzH` |
| `appsscript.json` | access: MYSELF / executeAs: USER_ACCESSING / V8 / Asia/Tokyo |

---

## 4. 主要Markdown

- `PROJECT_STATUS.md`（最終更新 2026-05-28 / TASK-PORTAL-LINK-AUDIT-003 CLOSED @17）
- `SPEC.md`（金額計算仕様・§10 多部位逓減・§11 長期減額・§18 近接部位・医師同意）
- `docs/JYU_GAS_SOURCE_OF_TRUTH_2026-05-14.md`（**正本 = GitHub main** / clasp push 安全ゲート）
- `docs/CLAIM_APPLICATION_OUTPUT_ROUTE_DECISION_2026-05-07.md`（**正ルート = B案 Cloud Run Excel** 確定）
- `docs/CLAIM_APPLICATION_B_PLAN_REDISCOVERY_2026-05-07.md`（GCP / Cloud Run 構成）
- `docs/WEB5_PRODUCTION_MONTHLY_FLOW_CHECK_2026-05-08.md` ほか WEB1〜WEB6 設計 / LiveCheck 記録 計23本

**過去記録に出ていたが実在しないファイル（実測）:**

| 候補名 | 実測 |
|---|---|
| `docs/JREC-01_申請書様式運用メモ.md` | 存在しない |
| `docs/JREC-01_CloudRun_デプロイ手順.md` | 存在しない |
| `docs/JREC-01_出力物テンプレート管理.md` | 存在しない |
| `docs/JREC-01_TEMPLATE_INDEX.md` | 存在しない |
| `ROADMAP.md` / `NEXT_ACTIONS.md`（このrepo直下） | 存在しない（workspace ルートには別途あり） |

→ Cloud Run 手順・テンプレ管理は独立 md ではなく `CLAIM_APPLICATION_*` 系 docs と `tools/claim-excel/README.md` に分散記録されている。

---

## 5. スプレッドシート確認結果

| 項目 | 値 |
|---|---|
| name | **未確認**（container-bound のため表示名がコードに無い） |
| URL | **未特定** |
| ID | **未特定**（コード内に本体SS-ID 定数なし。`getActiveSpreadsheet()` 経由で動作） |
| 主要シート | 設定 / 来院ケース / 患者マスタ / 患者画面 / 施術明細 / 来院ヘッダ / 初検情報履歴 / 保険者情報 / 自費明細 / `_申請書生成ログ` |
| 受傷エピソード | "シート" ではなく caseKey = `患者ID_エピソード開始日_C{caseNo}` の計算値 |
| Dashboard | 本SS内に無し（Run_Log / Task_Queue / Projects は workspace の AIOS Dashboard 側） |

コード内に登場する外部SS-ID（JREC本体ではない）:
- `JBIZ_SS_ID = 1FnJdALwFSv48WiD6NWr0DzG78kwB692R2pFeiTcZlCc` … 価格マスタ参照用（慢性疼痛管理表 / JBIZ）
- 生成済み申請書xlsxの一例: `https://docs.google.com/spreadsheets/d/1HHv-KH6bmfA-vEZErZ94uZxLutsxGi-C/edit`

---

## 6. GAS構成

| 関数 | 役割 |
|---|---|
| `saveVisit_V3` | Sheets UI からの来院保存 |
| `saveVisitFromWeb_V3` | Web版 来院保存（UIシート非依存） |
| `calcEpisodeForCase_` | 30日ルール区分判定 |
| `calcHeaderAmountsByVisitKey_V3_` / `calcOnePartAmount_V3_` / `calcLongTermCoef_V3_` | 金額・長期減額算定 |
| `upsertDetailRows_V3_` | 施術明細書込 |
| `V3TR_buildTransferDataForMonth_` | 申請書転記データ生成 |
| `V3TR_menuGenerateApplication_B` | ★B案 申請書出力（正ルート） |
| `generateClaimApplicationBFromWeb_V3` | Web UI から B案呼出 |
| `srGenerateDocument` | 施術録 Docs 生成 |
| `doGet` | page= ルーティング（home / search / detail / visitNew / selfpay / monthlyClaims / monthlyClaimDetail） |

**deployment（2026-05-28 時点）:**
- staff entry: `AKfycbxODNWJ...` **@17**（公開導線・URL不変）
- KPI: `AKfycbxNMVV...` **@15 に pin**（`?action=insuranceKpiSummary` handler は Editor 上にのみ存在し HEAD 未commit。HEAD deploy すると handler 消失 → JBIZ連携破壊）

**未ヒット関数（指定リストのうち本確認で grep 未ヒット・要個別確認）:**
`buttonSavePatientScreen` / `buttonClearPatientScreen` / `setupPatientScreenButtons_V3` / `inspectPatientScreenButtons_V3` / `clearEntryUI_V3`（旧UI系の可能性）

---

## 7. Cloud Run / Python構成

| 項目 | 値 |
|---|---|
| 使用有無 | ✅ 使用中（B案＝正ルート / 申請書 xlsx 出力の正本経路） |
| Cloud Run URL | `https://jrec-appgen-server-j6vlxdvqaa-an.a.run.app` |
| revision | 00026-wv2（2026-04-20 デプロイ） |
| GCP project | `hirayama-jrec-appgen` / Secret Manager: `JREC_APPGEN_SECRET_KEY` |
| GAS scriptプロパティ | `APPGEN_ENDPOINT` + `APPGEN_SECRET`（運用中・HTML/JSに出さない） |
| ローカル補助 | `tools/claim-excel/write_application.py`（Cloud Run と同一ロジック） |
| 正式帳票テンプレ | `tools/claim-excel/application_template.xlsx`（新 様式第5号・現役） |
| server.py | このrepoに無し（Cloud Run 側 jrec-appgen-server に存在） |
| deploy状態 | 未deploy疑いなし（B案は GAS deploy 対象外。Cloud Run側も今回スコープ外） |

---

## 8. 制度ロジック棚卸し

| 制度項目 | 実装状況 | 根拠 | 懸念・次作業 |
|---|---|---|---|
| 30日ルール | 実装済み | Ver3_core.js `calcEpisodeForCase_` (3345) | なし |
| 初検/再検/後療 | 実装済み | Ver3_core.js 区分判定・`readCaseKubun_` / 初検情報履歴 upsert | なし |
| 同日複数case | 実装済み | Ver3_core.js Case1/Case2 処理・caseNo 管理 | なし |
| 多部位逓減 | 実装済み | Ver3_amounts.js §10 (587)・金属副子/運動後療は逓減対象外 | なし |
| 長期減額 75%/50% | 実装済み | Ver3_amounts.js `calcLongTermCoef_V3_` (336) → 0.75/0.50・骨折/不全骨折は対象外 | なし |
| 月内上限制御 | 実装済み | Ver3_amounts.js §3-6 月内上限ルール (850) | なし |
| 近接部位制御 | 実装済み | Ver3_core.js §18 近接部位チェック（検出時**保存ブロック** 1249）/ SPEC §18 | なし |
| 医師同意管理 | **一部実装（仕様のみ）** | SPEC.md §539/§557 に規定（骨折・脱臼で要同意・摘要欄記載）。**.js に自動化ロジック無し** | 摘要欄への医師氏名・同意年月日は人手入力前提。自動チェック/必須化は未実装＝将来課題候補 |
| 申請書明細生成 | 実装済み | Ver3_transferData.js `V3TR_buildTransferDataForMonth_` (367) | なし |
| 施術録生成 | 実装済み | Ver3_shuRecorder.js `srGenerateDocument`（Docsテンプレ複製） | テンプレID=設定シート「施術録テンプレートID」要設定 |
| 抑制理由ログ | 実装済み | Ver3_amounts.js `reasons.push(...)` / 来院ヘッダ `needCheckReason` | なし |
| 監査ログ | **限定実装** | `_申請書生成ログ` シート（Ver3_transferData.js 3055）で申請書生成を記録。汎用操作監査ログは無し | 来院登録/削除の監査ログは未整備＝必要なら将来課題 |

---

## 9. live-check-runner / read-only HTTP 確認結果

**live-check-runner 実体:** `C:\hirayama-ai-workspace\workspace\tools\live-check-runner`（jyu-gas-ver31 プロジェクト・spec 21本 / config.json: devUrl・@13 prodUrl・@15 KPI URL）

**read-only HTTP 検証（認証不要エンドポイントのみ・2026-06-03 実測）:**

| 確認 | 結果 |
|---|---|
| Cloud Run `/health` | `{"status":"ok"}` ✅ |
| insuranceKpi `@15`（ANYONE_ANONYMOUS / GET のみ） | HTTP 200 / `{"ok":true, target_month:"2026-06", insurance_visit_count:0, ...}` ✅（6月の保険来院がまだ0件＝正常。@15 rollback 維持を確認） |
| staff prod `/exec`（MYSELF access） | ログインリダイレクト（想定通り・中身は未確認） |

**Playwright スイート本体は今回も未実行。理由:**
- auth.json が 2026-05-28 付（6日前）→ Google storageState 失効の可能性大
- Chrome CDP port 9222 未起動（要 Chrome 再起動＋人手 Google 再ログイン）
- 別 claude プロセス並行稼働 → 共有 auth.json / CDP の single-writer 競合リスク
→ 確認/記録フェーズかつ「本番書込禁止・人手最小化」方針に基づき見送り。

---

## 10. 本番書込につながる処理（今回は未実行）

| 処理 | 書込先 | 危険度 |
|---|---|---|
| `saveVisitFromWeb_V3` / `saveVisit_V3` | 来院ケース・来院ヘッダ・施術明細シート | 本番データ追加 |
| `V3TR_menuGenerateApplication_B` / `generateClaimApplicationBFromWeb_V3` | Cloud Run 実行＋Drive保存＋`_申請書生成ログ` 追記 | Drive/ログ書込 |
| `srGenerateDocument` | Drive に施術録 Docs 生成 | Drive書込 |
| `clasp push` / `clasp deploy` | GAS production code | デプロイ |

---

## 11. 触ってよい / 触ってはいけないファイル

**触ってよい（実装時の主対象）:**
`Ver3_transferData.js`（申請書/B案 wrapper）/ `Ver3_core.js`（doGet/来院登録）/ `Ver3_amounts.js`（金額）/ `web-*.html` / `Ver3_shuRecorder.js`（施術録）

**触ってはいけない（破壊リスク）:**
- KPI deployment（`AKfycbxNMVV...` @15）への HEAD code deploy → action handler 消失で JBIZ 連携破壊（**@15 pin 維持**）
- A案 `generateClaimApplication_V3`（停止経路・復活させない）
- `tools/claim-excel/application_template.xlsx`（帳票崩れ直結）
- 認証情報（APPGEN_SECRET を HTML/JS に出さない）

---

## 12. 未特定事項

```
未特定事項:
- JREC-01 本体スプレッドシートの URL / ID / 表示名
- container-bound script のためコード内には本体SS IDが存在しない
- 確定には GAS editor / clasp open 等での確認が必要
```

確認方法・判断基準:
- 確認方法: GAS editor（スクリプトから「コンテナを開く」）または `clasp open`（scriptId `1LROlc63TPr4...`）でバインド先 SS を開く
- 判断基準: 来院ケース / 患者マスタ / 来院ヘッダ / 施術明細 等の主要シートが存在する SS が JREC-01 本体

---

## 13. 次に進むべき作業

1. **JREC-01 本体スプレッドシート URL/ID を特定**し PROJECT_STATUS / live-check-runner config に明記（現状唯一の未特定情報）
2. **live-check-runner auth 更新**（6日前で失効疑い・single writer 確認後）→ `npm run test:jyu:smoke` 等で @13/@17 既存導線の無破壊を read-only 確認
3. **follow-up TASK-PORTAL-13-INSURANCE-KPI-HANDLER-COMMIT**: Editor 上にのみ存在する `?action=insuranceKpiSummary` handler を `Ver3_core.js` に正式 commit → KPI deployment への HEAD deploy 安全化・dual-deploy parity 回復
4. （任意）医師同意の摘要欄チェック自動化・来院登録の監査ログ整備（制度ロジック残課題）

---

## 14. 次回再開情報

```
repo:   C:\hirayama-ai-workspace\workspace\gas-projects\jyu-gas-ver3.1
branch: main
HEAD:   a90c4d6（本記録時点。次回 git pull --ff-only で最新化）
正本:   GitHub dabu-pi/jyu-gas-ver3.1 の main
正ルート(申請書): B案 Cloud Run Excel（V3TR_menuGenerateApplication_B）
deployment: staff @17 / KPI @15(pin)
未特定: JREC-01 本体スプレッドシート URL/ID/表示名
次作業候補:
  1. 本体SS URL/ID 特定
  2. live-check-runner auth 更新 → smoke read-only 検証
  3. insuranceKpi handler の正式 commit（dual-deploy parity 回復）
```
